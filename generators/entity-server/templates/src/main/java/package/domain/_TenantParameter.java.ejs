package <%=storage.packageName%>.domain;

import org.springframework.context.annotation.Scope;
import org.springframework.context.annotation.ScopedProxyMode;
import org.springframework.stereotype.Component;

import java.io.Serializable;

@Component
@Scope(value = "request", proxyMode = ScopedProxyMode.TARGET_CLASS)
public class <%= tenant.entityClass %>Parameter implements Serializable
{

    private <%= tenant.entityClass %> <%= tenant.entityInstance %>;

    public <%= tenant.entityClass %> get<%= tenant.entityClass %>() {
        return <%= tenant.entityInstance %>;
    }

    public void set<%= tenant.entityClass %>(<%= tenant.entityClass %> <%= tenant.entityInstance %>) {
        this.<%= tenant.entityInstance %> = <%= tenant.entityInstance %>;
    }
}
